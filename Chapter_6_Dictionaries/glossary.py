glossary = {'print': 'Its a function that print on the command line',
			'if statments':'These are a condicional flowchart',
			'while': 'This is a bucle that works when a statment is true',
			'for': 'This is a bucle that works just a finite repeticions',
			'get()': 'Is a method to access values in a dictionary without get errors',
			'set()': 'Is a function make a list of one element if ther is more than one repeted',
			'sorted()':'is a function that return a list sorted alphabetically',
			'keys()':'return a list of keys of a dictionary',
			'values()': 'return a list of values of a dictionary',
			'items()': 'return both, keys and values of a dictionary',      
			}

for key,value in glossary.items():
	print(f"{key}: \n\t{value}\n")

